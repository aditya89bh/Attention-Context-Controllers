[project]
name = "attention-context-controllers"
version = "0.1.0"
description = "Deterministic attention and context control primitives for AI agent research."
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Aditya" }
]
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
